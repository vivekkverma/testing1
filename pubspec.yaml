name: flutter_news_app
description: A Flutter news app

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  cached_network_image: ^3.2.0
  url_launcher_web: ^2.0.2
  provider: ^6.0.1
  flutter_sentiment_analysis: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
